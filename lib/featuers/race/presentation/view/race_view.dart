import 'package:flutter/material.dart';

class RaceView extends StatelessWidget {
  const RaceView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Race View'),
      ),
    );
  }
}
